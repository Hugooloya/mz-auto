---

---

<section id="about" class="about">
    <div class="container">
        <div class="content">
            <h2>About MZ Auto Detail</h2>
            <p class="highlight-text">
                We offer a 10% discount for all new customers!
            </p>
            <p>
                MZ Auto Detail is dedicated to providing the highest quality
                automotive detailing services. We use only the best high-end
                products to ensure your vehicle looks its absolute best. With
                years of experience, we have mastered the art of detailing,
                treating every car as if it were our own.
            </p>
            <a href="#contact" class="btn">Get in Touch</a>
        </div>
        <div class="image-wrapper">
            <img
                src="https://images.unsplash.com/photo-1552934214-48e7e4ae912b?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80"
                alt="Detailing in action"
            />
        </div>
    </div>
</section>

<style>
    .about {
        padding: 5rem 0;
        background-color: #222;
    }
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: center;
    }
    h2 {
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }
    .highlight-text {
        font-size: 1.25rem;
        color: var(--color-accent);
        font-weight: 700;
        margin-bottom: 1.5rem;
    }
    p {
        color: var(--color-text-muted);
        line-height: 1.8;
        margin-bottom: 2rem;
    }
    .image-wrapper img {
        width: 100%;
        border-radius: 8px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    }
    .btn {
        display: inline-block;
        padding: 0.8rem 2rem;
        background-color: transparent;
        border: 2px solid var(--color-accent);
        color: var(--color-accent);
        text-decoration: none;
        font-weight: 600;
        border-radius: 50px;
        transition: all 0.3s;
    }
    .btn:hover {
        background-color: var(--color-accent);
        color: white;
    }

    @media (max-width: 968px) {
        .container {
            grid-template-columns: 1fr;
            gap: 2rem;
            text-align: center;
        }
        .image-wrapper {
            order: -1;
        }
    }
</style>
